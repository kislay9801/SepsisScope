# Loaded by Next.js in dev mode only (next dev).
# Vercel production builds do NOT use this file, so the frontend
# falls back to the relative /api/analyze path (Python serverless function).
NEXT_PUBLIC_API_URL=http://localhost:5000/api/analyze
